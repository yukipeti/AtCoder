#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a,b;
    string s;

    cin >> a;

    vector<string> x(a);
    vector<int> y(a);

    for(int i = 0; i <a; i++)
    {
        cin >> s;
        cin >> b;

        x[i]=s;
        y[i]=b;

        if
    }

    return 0;
}