#include <bits/stdc++.h>
using namespace std;

int main(){
    int N,s;
    vector<char> S;


    cin >> N;
    for(int i = 0;i < N;i++){
        cin >> s;
        S.push_back(s);
    }
    for(int i = 0;i < N;i++){
        if(S[i + 1] != S[i]){
        }
    }

    return 0;
}